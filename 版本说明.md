# DshController v2.1.1（2026-09-08）

对应 git tag：`v2.1.1`；源码与 Windows x64 正式版产物由同一次发布构建生成。

## 本版要点

- 档案页升级为统一用量工作台：总量与单实例详情共享五张 KPI、日期范围、趋势图、模型排行与会话明细。
- 趋势图采用平滑渐变曲线、日期 Tooltip、竖向指示线、节点高亮与数据表；支持日、近 7 天、周、月、自定义与全部时间。
- 从 DSH 会话时序统计平均首 Token 延迟（TTFT），并按会话/档案/日期保存。
- 四桶 Token 全为 0 的空会话不再落档、导入、计数或显示；会话列表显示原始标题，展开详情可查看完整标题。
- API 预设模型目录新增图片、视频、音频能力三态，探针与同步保留明确声明。

## 数据位置（升级不动数据）

- 实例清单、预设、档案和日志：`%LOCALAPPDATA%\DshController\`。
- 实例会话数据：各实例的 `DSH_HOME`。
- 本发布仅替换程序文件与干净源码快照，不触碰以上运行时数据。
