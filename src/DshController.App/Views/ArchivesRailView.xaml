<?xml version="1.0" encoding="utf-8"?>
<!-- Cockpit 风格档案轨道：仍只读取 ArchivesRailViewModel 的档案快照。 -->
<UserControl x:Class="DshController.Views.ArchivesRailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:DshController.Views" xmlns:vm="using:DshController.ViewModels" mc:Ignorable="d">
    <UserControl.Resources><views:BoolToVisibilityConverter x:Key="RailBoolToVis" /></UserControl.Resources>
    <StackPanel Spacing="10">
        <Border CornerRadius="12" Padding="12,10" Background="{ThemeResource BrandSoftBrush}" BorderBrush="{ThemeResource BorderBrushToken}" BorderThickness="1">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel Spacing="2"><TextBlock Text="档案" FontWeight="SemiBold" Foreground="{ThemeResource LabelPrimaryBrush}" /><TextBlock Text="全部实例与历史快照" Style="{StaticResource HelperText}" /></StackPanel><StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="{x:Bind ViewModel.ActiveCount}" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="{ThemeResource BrandPrimaryBrush}" /><TextBlock Text="{x:Bind ViewModel.CountsText}" Style="{StaticResource HelperText}" /></StackPanel></Grid>
        </Border>
        <ListView x:Name="RailList" SelectionMode="Single" BorderThickness="0" Background="Transparent" Padding="0" ScrollViewer.VerticalScrollBarVisibility="Disabled" AutomationProperties.AutomationId="ArchivesRailList" SelectionChanged="RailList_SelectionChanged">
            <ListView.Resources><SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="{ThemeResource BrandSoftColor}" /><SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="{ThemeResource BrandSoftColor}" /><SolidColorBrush x:Key="ListViewItemForegroundSelected" Color="{ThemeResource BrandPrimaryColor}" /></ListView.Resources>
            <ListView.ItemContainerStyle><Style TargetType="ListViewItem"><Setter Property="MinHeight" Value="0" /><Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0,2" /><Setter Property="HorizontalContentAlignment" Value="Stretch" /></Style></ListView.ItemContainerStyle>
            <ListView.ItemTemplate><DataTemplate x:DataType="vm:ArchivesRailRow"><Grid ColumnSpacing="8" Opacity="{x:Bind RowOpacity}" ToolTipService.ToolTip="{x:Bind Tooltip}"><Grid.ColumnDefinitions><ColumnDefinition Width="4" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><Border Grid.Column="0" CornerRadius="2" Background="{ThemeResource BrandPrimaryBrush}" Visibility="{x:Bind IsTotals, Converter={StaticResource RailBoolToVis}}" /><StackPanel Grid.Column="1" Margin="8,7,4,7" Spacing="2"><StackPanel Orientation="Horizontal" Spacing="6"><FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE9D2;" FontSize="12" Foreground="{ThemeResource BrandPrimaryBrush}" Visibility="{x:Bind IsTotals, Converter={StaticResource RailBoolToVis}}" /><TextBlock Text="{x:Bind Title}" FontSize="12.5" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource LabelPrimaryBrush}" /></StackPanel><TextBlock Text="{x:Bind SubText}" FontSize="10.5" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource LabelTertiaryBrush}" /></StackPanel><Border Grid.Column="2" VerticalAlignment="Center" CornerRadius="6" Padding="6,2" Background="{ThemeResource BrandSoftBrush}" Visibility="{x:Bind IsRetired, Converter={StaticResource RailBoolToVis}}"><TextBlock Text="{x:Bind Badge}" FontSize="9.5" Foreground="{ThemeResource LabelSecondaryBrush}" /></Border></Grid></DataTemplate></ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
